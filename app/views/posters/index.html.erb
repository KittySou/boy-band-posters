<h1>Posters</h1>

<div class="container">

  <div class="row justify-content-center">
    <div class="col-sm-8 my-3">
      <%= form_with url: posters_path, method: :get, class: "d-flex" do %>
        <%= text_field_tag :query,
          params[:query],
          class: "form-control",
          placeholder: "Type your favourite boys band"
        %>
        <%= submit_tag "Search", name: "", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>

</div>

<div class="poster-index">
  <% @posters.each do |poster| %>
    <%= link_to poster_path(poster) do %>
      <div class="card-product">
        <%= cl_image_tag poster.photo.key %>
        <div class="card-product-infos">
          <h1 class="card-title">
          <%= poster.band_name %>
          </h1>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

<%# INFO: The code below is for references only. Delete when no longer needed. %>
<%# if poster.photo.attached? %>
  <%# cl_image_tag poster.photo.key, height: 400, width: 250, crop: :fill %>
<%# end %>
<
