
<div class="poster-index">
  <% @posters.each do |poster| %>
    <%= link_to poster.band_name, poster_path(poster) %>
    <%= cl_image_tag poster.photo.key, height: 400, width: 250, crop: :fill %>
  <% end %>
</div> %>

<div class="poster-index">
 <% @posters.each do |poster| %>
    <%= cl_image_tag poster.photo.key, height: 400, width: 250, crop: :fill %>
    <div class="poster-index-infos">
      <h2><%= link_to poster.band_name, poster_path(poster) %></h2>
    </div>
    <% end %>
</div>

<div >
  <% @posters.each do |poster| %>
    <%= link_to poster.band_name, poster_path(poster) %>
    <%= cl_image_tag poster.photo.key, height: 400, width: 250, crop: :fill %>
  <% end %>
</div>



<div class="poster-index">
  <% @posters.each do |poster| %>
    <%= cl_image_tag poster.photo.key, height: 400, width: 250, crop: :fill %>
    <div class="poster-index-infos">
      <h2><%= link_to poster.band_name, poster_path(poster), class:"btn btn-flat" %></h2>
    </div>
  <% end %>
</div>
