<div class="text-center padded">
  <h1>Bookings</h1>
</div>
<div class="text-center">
  <h4 id="red-word">My future bookings</h4>
</div>

<div class="wrapper-dashboard">
  <ul class="list-unstyled">


<%= form_with model: @list do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.submit %>

<% end %>
