<div class="text-center padded">

<div class="container">
  <h1>#Checkout!</h1>

  <% if @bookings.present? %>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>ID</th>
          <th>Data de Check-In</th>
          <th>Data de Check-Out</th>
          <th>Valor</th>
          <th>Status</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @bookings.each do |booking| %>
          <tr>
            <td><%= booking.id %></td>
            <td><%= booking.checkin_on %></td>
            <td><%= booking.checkout_on %></td>
            <td><%= booking.value %> €</td>
            <td><%= booking.status %></td>
            <td>
              <%= link_to "Detalhes", booking_path(booking), class: "btn btn-info" %>
              <%= link_to "Excluir", booking_path(booking), method: :delete, data: { confirm: "Tem certeza que deseja excluir esta reserva?" }, class: "btn btn-danger" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p>YAY! Your booking is almost guaranteed 😎</p>
  <% end %>

    <%= link_to "Confirm your booking!", new_booking_path, class: "btn btn-primary" %>
  </div>
</div>
